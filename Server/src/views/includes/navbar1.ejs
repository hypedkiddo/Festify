<nav class="navbar navbar-expand-lg bg-light border-bottom sticky-top shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand text-primary fw-bold" href="/api/v1/events/list">
            <i class="fa-solid fa-compass"></i> Festify
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link text-secondary" href="/api/v1/events/list">Discover Events</a>
            </div>
            <div class="navbar-nav ms-auto">
                <form method="GET" action="/listings/search/list" class="d-flex" role="search">
                    <input class="form-control me-2 rounded-pill border-secondary" name="search" placeholder="Search Events" aria-label="Search">
                    <button class="btn btn-primary rounded-pill" type="submit">Search</button>
                </form>
            </div>
            <div class="navbar-nav ms-auto d-flex align-items-center">
                <% if (!currUser) { %>
                    <form action="/api/v1/users/logout" method="POST">
                        <button type="submit" class="btn btn-danger rounded-pill ms-2">Log out</button>
                    </form>
                <% } %>
            </div>
        </div>
    </div>
</nav>

<style>
    .navbar {
        font-family: 'Arial', sans-serif;
    }
    .navbar-brand {
        font-size: 1.5rem;
    }
    .nav-link {
        margin-right: 10px;
    }
    .form-control {
        max-width: 300px;
    }
    .btn {
        transition: background-color 0.3s ease;
    }
    .btn:hover {
        background-color: #0056b3;
        color: #fff;
    }
    .btn-outline-primary:hover {
        background-color: #0056b3;
        color: #fff;
        border-color: #0056b3;
    }
</style>
